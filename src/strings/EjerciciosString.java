package strings;

public class EjerciciosString {

	public static void main(String[] args) {
		/* 1 --> declara e inicializa 3 Strings, una contenente tu nombre y 
		 * la otras dos tus apellidos 
		 * (si solo tienes un apellido, el tercer String deberá contener "-").
		 * Sucesivamente concatena las tres cadenas y enseña el nombre completo 
		 * de la siguiente forma:
		 * nombre|apellido1|apellido2.
		 * Tip: en Java, el carácter "|" es especial
		 */
		
		/* 2 --> Divide el String anterior sobre el carácter separador "|" 
		 * de manera tal que obtengar un
		 * array de 3 valores.
		 * Enseña los 3 valores usando el array que has obtenido.
		 */
		
		/* 3 --> 
		 * Siempre usando los valores del array, controla si el nombre 
		 * empieza por "C", el apellido1
		 * termina por "s" y si el apellido2 contiene "ñ".
		 * Enseña los resultados.
		 * Recuerda, Java es un lenguaje case sensitive, así que los 
		 * caracteres "C" y "c" no son lo mismo   
		 */
	}

}
